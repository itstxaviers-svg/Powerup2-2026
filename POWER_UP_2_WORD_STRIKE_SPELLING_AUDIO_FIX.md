MODEL TO USE: GPT-5.6 Sol

Continue from the current Power Up 2 implementation and update ONLY the Word Strike game according to the current approved design. Do not modify Repair, Error Hunt, Audio Code, Code Fighter, Unit 1 vocabulary content, Unit Part selection, Weak Word Engine, module coverage logic, city construction, rewards, avatars, avatar evolution, registration, or Units 2–9.

Use the existing transparent gameplay asset `word-strike-fps-cannon.png` as the only cannon visible inside Word Strike gameplay. Keep the cannon in the same lower-right gameplay position where it is currently shown. Do NOT mirror, flip, rotate, or horizontally reflect the asset. Preserve the original orientation of the uploaded PNG exactly. Remove any CSS such as `scaleX(-1)`, negative transforms, mirrored background positioning, or any other logic that reverses the cannon. Render it with preserved aspect ratio and transparent background. It may extend slightly beyond the bottom/right viewport edge like an FPS weapon, but the visible muzzle, energy core and main body must remain clear. Do not move the cannon to the center. The current lower-right FPS placement is correct; only its accidental reflection/orientation must be fixed.

The Word Strike gameplay identity must now use TWO task types only: `SPELLING STRIKE` and `AUDIO STRIKE`. Stop scheduling Definition → Word, Translation → Word, Sentence Gap → Word and Picture → Word inside Word Strike for now. Do not delete those vocabulary fields from UnitWord because other modules may still need them and they may be reused later. This change is only to the active Word Strike task rotation.

SPELLING STRIKE must train visual spelling recognition. Select one canonical vocabulary word or phrase from the currently active Unit/Part vocabulary pool. Create exactly three visible targets for that same vocabulary item: one target contains the correct canonical spelling and two targets contain plausible configured spelling-error forms of the SAME word/phrase. Use the existing Unit 1 `typoVariants` / spelling-error forms as the source of the incorrect forms. Do not use unrelated vocabulary words as distractors in Spelling Strike. Do not use grammar errors, translations, definitions, punctuation errors, or changed word order. The learner must shoot the correctly spelled form.

For example, if the target is `mountain`, the three targets may be `mountain`, `mountan`, and `moutain`; if the target is `recycle`, show `recycle` plus two configured misspellings. For multiword phrases, keep the phrase structure and word order intact. Each incorrect target should contain a spelling error in only one component whenever practical, exactly in line with the existing Error Hunt spelling-only rules. For example, `pick up rubbish` may be shown together with forms such as `pick up rubish` and another configured spelling variant, while the correct phrase remains unchanged. Do not generate a typo dynamically if the UnitWord already has configured spelling variants unless a safe fallback is genuinely required.

Randomize the position of the correct spelling among the three targets. The correct answer must not consistently appear left, center, or right. Avoid repeating the same target position unnecessarily. Preserve the current target movement/interaction system as long as it remains usable and does not make spelling text unreadable.

AUDIO STRIKE must train listening recognition. Select one canonical vocabulary word or phrase from the currently active Unit/Part pool that has a working production audio source. Show three target choices containing correctly spelled English vocabulary items, with exactly one target matching the audio. The two distractors must be other plausible English vocabulary items from the currently active eligible pool, not misspellings of the target. The learner hears the target and shoots the correctly matching written form. Do NOT display the target word anywhere else in the prompt before the learner answers. The task header may say something concise such as `AUDIO STRIKE` and `Listen and strike the word you hear.` but must not reveal the answer.

Audio playback in AUDIO STRIKE must be automatic and delayed. Do NOT play the audio immediately at the instant the task screen/rerender appears. After a new Audio Strike task becomes fully visible and ready, wait approximately 1.5 seconds before the first automatic playback. After that, if the learner has not answered yet, automatically replay the same target audio every 7 seconds. The 7-second interval is measured between playback starts. Continue automatic replay only while that Audio Strike task is active and unanswered.

Keep a manual `Replay audio` / speaker control available so the learner can replay the word on demand without waiting for the next automatic interval. Manual playback must not create overlapping sounds. If the learner presses Replay while audio is already playing, stop/restart or safely restart the current clip using the existing audio architecture. After manual replay, reset the automatic 7-second countdown so the next automatic playback does not fire immediately afterward.

All audio timers and audio instances must be cleaned up correctly. When the learner answers, when the task advances, when Word Strike unmounts, when the selected Unit/Part changes, or when the browser tab/page loses the active task, stop the old audio and clear its delayed-start/replay timers so the previous word cannot play during the next question. Never allow audio from two tasks to overlap. Do not create multiple unmanaged intervals on rerender. Use one controlled timeout/interval lifecycle tied to the active Audio Strike task ID.

If audio cannot be played because a production asset is missing or fails to decode, do not silently mark the learner wrong and do not show a broken task. Use the existing recoverable audio error handling, allow retry where appropriate, and do not include non-audio-eligible words in Audio Strike selection if the eligibility system can filter them out beforehand.

The first delayed playback should occur only after the gameplay view is ready; it must not fire during route transition or before the targets are visible. If browser autoplay restrictions prevent automatic audio playback before any user interaction, handle this gracefully using the existing user-gesture/audio-unlock architecture. Do not replace production audio with browser speechSynthesis. Once audio playback has been unlocked by the learner's interaction with the app, subsequent Audio Strike tasks should follow the delayed first-play + 7-second auto-replay behavior.

Mix SPELLING STRIKE and AUDIO STRIKE within normal Word Strike sessions. Keep the distribution balanced enough that both mechanics are actually practised. Prefer a simple approximately 50/50 mix across a session/history while avoiding long runs of the same type where the current scheduler allows it. Do not hardcode a fragile sequence. A session may still contain the existing number of tasks; do not change module-coverage semantics. One short session is still only training, not full module completion.

For Word Strike module coverage, continue counting unique vocabulary IDs encountered and answered according to the existing corrected module-coverage system. A word counts once for Word Strike coverage after the learner commits an answer in either Spelling Strike or Audio Strike. Do not require both Word Strike subtypes for the same word to count toward coverage. Repeated encounters of the same word must not inflate unique coverage. Wrong answers still count as exposure for coverage but must continue to feed the existing Weak Word/recognition-review logic according to current rules.

Keep the Unit Part selector respected. Word Strike must first filter Unit vocabulary by the learner's selected sourcePart values, then apply Word Strike subtype eligibility. Spelling Strike requires canonical spelling plus at least two usable spelling variants. Audio Strike requires a valid production audio source and enough eligible distractors from the selected pool. Do not allow words from unselected Parts to leak into normal distractor selection or target selection. Due Weak Word behavior should remain consistent with the existing filtered-session rules.

Keep the existing Word Strike arena background and the current transparent floating target style. Do not reintroduce white rectangular/square target backgrounds. Each target must remain a clean floating circular/magical target integrated into the arena. Long words and phrases must wrap cleanly within the circular target without becoming unreadably small. Preserve a strong readable contrast for target text.

Keep the cannon in the CURRENT lower-right FPS position. Again: do not mirror it. The uploaded asset's original orientation is canonical. When the learner fires, use the existing cannon muzzle/projectile logic, but make sure the projectile begins at the actual visible muzzle position of this unmirrored lower-right cannon. A correct hit may trigger a short cyan/gold impact/success effect. An incorrect hit may trigger a short red/orange error effect. Keep recoil and muzzle flash brief. Temporary effects must disappear when the interaction ends and must not remain as permanent background decoration.

The top task panel must change according to subtype. For Spelling Strike, use concise learner-facing text such as `SPELLING STRIKE` and `Strike the correctly spelled word.` Do not show a definition or another clue that makes the answer trivial unless explicitly required later. For Audio Strike, use `AUDIO STRIKE` and `Listen and strike the word you hear.` Do not print the target word in the prompt. Accuracy/status UI may remain compactly in the upper-right safe area.

Do not show the old Definition Lock prompt inside the active Word Strike rotation after this change. Remove Definition/Translation/Sentence/Picture task scheduling from Word Strike UI/engine while retaining their underlying vocabulary data for other modules. Do not delete generic reusable helpers if other modules depend on them.

Respect Reduced Motion. When Reduced Motion is enabled, keep the cannon in the same position and orientation, eliminate or minimize recoil/projectile travel animation, and replace it with short muzzle/target state flashes. Audio timing behavior remains the same; Reduced Motion must not disable listening tasks or automatic replay.

Verify desktop and landscape tablet/phone layouts. The lower-right cannon must remain visible, unmirrored and correctly proportioned. The cannon must not cover the rightmost target, target labels, prompt, Accuracy UI or status controls. If necessary, constrain the target movement safe area rather than moving the cannon away from the approved lower-right position. No page-level horizontal overflow is allowed.

Before stopping, verify at minimum: `word-strike-fps-cannon.png` is displayed in its original orientation with no horizontal reflection; the cannon remains in the current lower-right FPS position; Spelling Strike always shows one correct canonical form plus two spelling-only variants of the same target; the correct target position is randomized; multiword phrases preserve word order and contain only spelling variation; Audio Strike shows three correctly spelled vocabulary choices and does not expose the answer in the prompt; the first automatic audio playback occurs after approximately 1.5 seconds rather than immediately; unanswered Audio Strike tasks replay automatically every 7 seconds; manual Replay works and resets the replay countdown; timers/audio are cleaned up on answer/task change/unmount; there is no audio overlap; Part filtering is respected; unique Word Strike coverage continues to work; Definition/Translation/Sentence/Picture tasks are no longer scheduled in Word Strike; targets remain transparent; and the other games/progression systems are unchanged.

Add or update focused tests for cannon orientation/no-mirror CSS, Word Strike subtype selection, Spelling Strike same-word typo targets, correct-position randomization, multiword spelling targets, Audio Strike distractor construction, hidden pre-answer target text, delayed initial autoplay, 7-second replay lifecycle, timer cleanup, manual replay/reset behavior, selected-Part filtering, and unique coverage compatibility. Use fake timers in tests for autoplay/replay behavior rather than waiting in real time. Run the relevant tests and one production build. Stop after this task and report the files changed, cannon orientation fix, final cannon positioning, active Word Strike task types, Spelling Strike target construction, Audio Strike target construction, initial playback delay, 7-second replay implementation, timer/audio cleanup, responsive behavior, test results, production build result, and confirmation that no unrelated game/vocabulary/progression logic was changed.
